{
  "name": "presenton-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "White-label reverse proxy for self-hosted Presenton (auth-inject + debrand + embeddable)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
